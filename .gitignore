__pycache__/
*.pyc
*.pyo
.env
.venv
*.egg-info/
data/

# graphify: local-only knowledge graph output, not shared with other clones
graphify-out/

# Working-notes plan docs — local scratch, not meant to ship with the repo
VIDEO_EDIT_TAB_PLAN.md
QWEN_CHAT_INTEGRATION_PLAN.md

.DS_Store
node_modules/

# Built new UI. Generated by `npm run build`; not committed (brief §「改路线」).
# The old UI under static/js and static/css is source and stays tracked.
static/newui/
static/newui.html

# Discarded hand-written M3 layer (route v1) kept locally as a behaviour
# reference only. Never imported, never served, never committed.
frontend/legacy-vanilla-views/
